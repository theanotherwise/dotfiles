# `.dotfiles`

## Initialize Repository

```bash
git -C "${HOME}" init && git -C "${HOME}" remote add origin https://github.com/theanotherwise/dotfiles.git && git -C "${HOME}" pull origin main && bash "${HOME}/.dotfiles"
```

#!/bin/bash

mkdir -p "${HOME}/etc/ssh/keys"
mkdir -p "${HOME}/cron.d"

mkdir -p "${HOME}/bin/ruby"
mkdir -p "${HOME}/bin/terraform"
mkdir -p "${HOME}/bin/helm"
mkdir -p "${HOME}/bin/node.js"
mkdir -p "${HOME}/bin/yarn"

mkdir -p "${HOME}/archives"
mkdir -p "${HOME}/downloads"
mkdir -p "${HOME}/scripts"
mkdir -p "${HOME}/scripts/init-vm"
mkdir -p "${HOME}/projects"
mkdir -p "${HOME}/git"
mkdir -p "${HOME}/tmp"
